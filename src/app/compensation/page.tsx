'use client';

import DashboardLayout from '@/components/layout/dashboard-layout';
import CompensationContent from './compensation-content';

export default function CompensationPage() {
  return (
    <DashboardLayout>
      <CompensationContent />
    </DashboardLayout>
  );
}
